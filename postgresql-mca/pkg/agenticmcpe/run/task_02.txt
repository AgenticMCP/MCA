I'm sketching out inventory tracking for a prototype. Set up a schema called agenticmcpe_bench_inventory with a table named items that has an integer id as primary key, a text column name, and an integer column qty. Seed it with exactly these three rows: (1, 'bolt', 100), (2, 'nut', 250), (3, 'washer', 75). Then show me the rows ordered by id, and the total quantity across all items as a column named total_qty.
