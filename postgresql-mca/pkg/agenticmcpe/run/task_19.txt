Hey, I'd love a read-only performance pass on this Postgres instance so I can see where time is actually going — no DDL or DML anywhere. Could you start by listing the schemas on the instance so the layout is on record, and then pull the detailed column metadata for the pg_stat_statements view so I know exactly which columns I'll be filtering against (calls, total_exec_time, mean_exec_time, query, etc.)?

Once that's in hand, please pull the top slow queries from pg_stat_statements twice — once ordered by total_exec_time DESC and limited to 10, and once ordered by mean_exec_time DESC and limited to 10 — so I can see total-time and per-call hotspots side by side. Then run a single-row SELECT that returns COUNT(*) over pg_stat_statements so I have a scalar on record for how many distinct statements are currently tracked.

As a companion catalog fact, run a labeled SELECT against pg_settings returning name and setting for `autovacuum` and `autovacuum_naptime` — just those two rows, ordered by name — so a concrete runtime fact is captured. Then, for a quick plan on record, run an EXPLAIN (no ANALYZE) on this concrete query: `SELECT current_database() AS db, current_user AS usr, version() AS server_version;` so I can see the planner's view of a trivial single-row lookup.

Round it out with the workload-wide index advisor over the recorded pg_stat_statements workload so I can see what it would add, and finish with a connection-scope database health check so anything else worth flagging — planner issues, vacuum lag, long transactions, buffer cache — gets surfaced before I call the review done. Pure inspection.
