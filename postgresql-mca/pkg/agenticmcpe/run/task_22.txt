Run a small audit-then-remediate drill against this Postgres instance and prove the fix. Start by listing the schemas on the instance, then enumerate the objects inside `public` so the current state is on record. Next, set up a fresh schema called `agenticmcpe_bench_auditfix_meridian` and inside it build a findings table with `id serial PRIMARY KEY`, `finding_code text`, `severity text`, `object_name text`, `recommendation text`, and `resolved boolean DEFAULT false`. Also inside that schema, build a seeded table `audit_targets` with `id serial PRIMARY KEY`, `code text`, `label text`, and `value numeric`, and seed it with exactly these four rows verbatim: (1, 'TX-01', 'turbine-a', 12.5), (2, 'TX-01', 'turbine-b', 14.0), (3, 'TX-02', 'turbine-c', 9.8), (4, 'TX-03', 'turbine-d', 22.1). Then insert exactly three literal findings into the findings table: (1, 'IDX-CODE', 'medium', 'audit_targets', 'create btree index on audit_targets(code)', false), (2, 'CHK-VALUE', 'low', 'audit_targets', 'add check constraint value > 0', false), (3, 'IDX-CODE-DUP', 'medium', 'audit_targets', 'create btree index on audit_targets(code)', false). Now apply the remediation: create a B-tree index `idx_audit_targets_code` on `audit_targets(code)` so both IDX-CODE findings can be marked resolved. Re-inspect by selecting schemaname, indexname, indexdef from pg_indexes filtered to the new schema, ordered by indexname, so the new index is provably registered. Then run ONE UPDATE that sets resolved=true on all findings whose finding_code starts with 'IDX-CODE' (two rows). Read the findings table back as a labeled SELECT showing finding_code, severity, object_name, resolved, ordered by id, so the resolution state is visible. Finish with a connection-scope database health check. Every DDL and DML must stay strictly inside `agenticmcpe_bench_auditfix_meridian` — do not touch `public` or any other schema.
