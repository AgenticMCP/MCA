Hey, I'm trying to get oriented on this Postgres instance before I plan anything — could you do a read-only catalog survey for me? Start by listing the schemas, then enumerate what's inside information_schema so I know which system views we're working with. After that, pull the detailed column metadata for information_schema.tables so I can see the columns I'll be joining to.

Then run a handful of labeled SELECTs (everything strictly system-catalog reads, no DDL or DML anywhere): one prefixed with `-- table_counts_per_user_schema` returning schema_name, table_count grouped from information_schema.tables (table_type='BASE TABLE', excluding pg_catalog and information_schema) ordered by table_count desc, limit 10; one prefixed with `-- total_user_table_count` returning the scalar COUNT(*) under the same predicate; one prefixed with `-- extensions_and_versions` selecting name and default_version from pg_available_extensions ordered by name so I can see what's installable; one prefixed with `-- runtime_identity` returning current_database(), current_user, current_setting('server_version') as a single row; one prefixed with `-- pg_settings_runtime` returning name and setting from pg_settings filtered to a small set of names (autovacuum, shared_buffers, work_mem, max_connections, statement_timeout) ordered by name; and one prefixed with `-- schema_size_summary` as a one-row rollup combining the current database size and a count of distinct schemas from information_schema.schemata. Wrap up with a connection-scope database health check so anything else worth flagging — planner issues, vacuum lag, long transactions — gets surfaced. Pure inspection please.
