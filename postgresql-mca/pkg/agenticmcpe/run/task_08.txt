Run a read-only query-plan review against the system catalogs: list the schemas, enumerate pg_catalog objects, verify metadata for pg_class, pg_namespace, and pg_settings, then compare the estimated and actual row counts for the heap-relation join with and without relpages > 0, corroborate both counts with scalar aggregate SELECTs, explain a bounded autovacuum pg_settings lookup without ANALYZE, and finish with a connection-scope health check; make no changes.
