# Python dependencies for the pkg/ tooling (agenticmcpe, mcp_wrapper, taskgen).
# Captured from pkg/venv via `pip freeze` (resolved under CPython 3.14).
#
# Recreate on another machine:
#   python3 -m venv pkg/venv
#   pkg/venv/bin/python -m pip install -r pkg/requirements.txt
#
# Direct dependencies (imported by the code): openai, anthropic.
# All other lines are transitive deps, pinned for reproducibility.

# --- direct ---
anthropic==0.97.0
openai==2.32.0

# --- transitive ---
annotated-types==0.7.0
anyio==4.13.0
certifi==2026.4.22
distro==1.9.0
docstring_parser==0.18.0
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.13
jiter==0.14.0
pydantic==2.13.3
pydantic_core==2.46.3
sniffio==1.3.1
tqdm==4.67.3
typing-inspection==0.4.2
typing_extensions==4.15.0