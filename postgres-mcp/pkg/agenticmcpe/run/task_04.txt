Hi! I'm stress-testing Postgres's index advisor against a moderately-sized scratch table before I roll it out to our log-search pipeline. Could you set me up a working table called `agenticmcpe_bench_logscan_demo` directly in the `public` schema (this is one of those advisor tasks where the table has to live in public so the advisor can find it) with these columns: `id serial PRIMARY KEY`, `level text`, `source text`, `message text`, and `created_on date`. Then bulk-seed exactly 2500 rows in a single `INSERT ... SELECT ... FROM generate_series(1,2500)` statement — cycle `level` through ('info','warn','error'), cycle `source` through ('api','worker','cron','web'), set `message` to `'msg-' || i::text`, and set `created_on` to `date '2025-02-01' + i`. Run `ANALYZE` on the table right after seeding so the planner and the advisor have real stats to work with.

Once the data is in, please ask the index advisor to analyze these two UNQUALIFIED queries (so they resolve against the public-schema table):

  1) `SELECT id, message FROM agenticmcpe_bench_logscan_demo WHERE level = 'error';`
  2) `SELECT id, message FROM agenticmcpe_bench_logscan_demo WHERE source = 'worker';`

Then create the indexes the advisor recommends (B-tree on `level` and B-tree on `source` is the expectation), ANALYZE the table again, and run `EXPLAIN (ANALYZE, BUFFERS)` for each of the two queries above so I can see the plan now uses the new indexes. Confirm the indexes are registered by querying `pg_indexes` filtered to `schemaname = 'public'` and returning rows for exactly the two indexes I expect, with their indexdef strings visible. Finish with a connection-scope database health check so I can see buffer-cache / vacuum / long-tx status before I sign off on the advisor's picks. Please keep every write in `public` (the advisor's convention) and don't create any extra schemas.
