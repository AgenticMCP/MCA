Hey, I'd love a quick read-only performance pass on this Postgres instance so I can see where time is actually going. Could you pull the top slow queries out of pg_stat_statements (the slowest ones by total/mean time is fine), and then alongside that do two simple companion reads — first run a single-row SELECT that returns the current setting for `autovacuum` and `autovacuum_naptime` from `pg_settings` so I have a concrete runtime fact on record, and second get the index advisor's recommendation over the recorded pg_stat_statements workload so I can see what it would add. While you're at it, run a one-row SELECT that counts how many distinct statements pg_stat_statements currently has recorded — I just want a scalar number. To round things out, please list the schemas on the instance, pull the detailed column metadata for pg_stat_statements so I know what's actually in that view, and finish with a connection-scope database health check. Pure inspection — no DDL or DML anywhere.
