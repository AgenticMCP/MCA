Hi! I'm staging a small upsert migration and want a tidy playground to demo the reconcile. Could you set up a fresh schema called `agenticmcpe_bench_mergesync_kiosk` and inside it build a `products` table with `id serial PRIMARY KEY`, `sku text UNIQUE`, `name text`, `stock_qty integer`, and `unit_price numeric`. Then seed it with these three rows verbatim, in this order: (default id, sku='SKU-001', name='widget', stock_qty=10, unit_price=4.50), (default id, sku='SKU-002', name='sprocket', stock_qty=25, unit_price=1.20), (default id, sku='SKU-003', name='gizmo', stock_qty=7, unit_price=9.99).

Once the seed is in, run a labeled SELECT prefixed `-- before_merge` that returns id, sku, name, stock_qty, unit_price ordered by id so I can see the initial state.

Then apply ONE `INSERT ... ON CONFLICT (sku) DO UPDATE` statement that reconciles an incoming batch: SKU-002 should be updated to name='sprocket-v2', stock_qty=40, unit_price=1.35 (the existing row gets overwritten), and SKU-004 should be inserted fresh as name='doodad', stock_qty=15, unit_price=2.75 (a brand-new row). After the merge, run a labeled SELECT prefixed `-- after_merge` returning id, sku, name, stock_qty, unit_price ordered by id so I can see SKU-002 updated and SKU-004 added, plus a labeled scalar SELECT prefixed `-- final_row_count` returning COUNT(*) over `products` so the final row count of 4 is exposed numerically.

To wrap up, list the objects in the new schema so the table is confirmed present, pull the detailed column/constraint metadata for `products` so I can see the UNIQUE constraint on `sku` is registered, and finish with a connection-scope database health check. Every DDL and DML must stay strictly inside `agenticmcpe_bench_mergesync_kiosk` — public is untouched.
