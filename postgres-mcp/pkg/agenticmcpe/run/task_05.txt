I'm stress-testing Postgres's index advisor on a scratch table before I trust it on a billing query. Set up a working table called `agenticmcpe_bench_billingscan_demo` directly in the `public` schema (advisor convention) with columns `id serial PRIMARY KEY`, `customer text`, `region text`, `amount_cents integer`, `created_on date`. Bulk-seed exactly 2500 rows in one `INSERT ... SELECT ... FROM generate_series(1,2500)` statement — cycle `customer` through ('acme','globex','initech','umbrella','wayne'), cycle `region` through ('east','west','north','south'), set `amount_cents` to `((i*173 + 91) % 10000)`, and set `created_on` to `date '2025-01-01' + i days`. Run `ANALYZE` on the table right after seeding.

Once the data's in, ask the index advisor to analyze these two UNQUALIFIED queries (they resolve against the public-schema table):
  1) `SELECT id, amount_cents FROM agenticmcpe_bench_billingscan_demo WHERE region = 'west';`
  2) `SELECT id, amount_cents FROM agenticmcpe_bench_billingscan_demo WHERE amount_cents > 5000;`

Create the B-tree indexes the advisor recommends (expectation: one on `region`, one on `amount_cents`), ANALYZE the table again, then run `EXPLAIN (ANALYZE, BUFFERS)` for each of the two queries above so I can see the plan now uses the new indexes. Confirm both indexes are registered by querying `pg_indexes` filtered to `schemaname = 'public'` and returning rows for exactly those two indexes with their indexdef strings visible. Finish with a connection-scope database health check. Keep every write in `public` per the advisor's convention — no extra schemas.
